input {
  beats {
    port => 5045   # changed from 5044
  }
  udp {
    port => 5515   # changed from 5514
    codec => json
  }
}

filter {
  if [type] == "wazuh" {
    mutate {
      add_field => { "[@metadata][index]" => "wazuh-%{+YYYY.MM.dd}" }
    }
  } else {
    mutate {
      add_field => { "[@metadata][index]" => "syslog-%{+YYYY.MM.dd}" }
    }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "%{[@metadata][index]}"
  }
}
